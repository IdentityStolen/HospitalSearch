services:
#  temporal:
#    image: temporalio/auto-setup
#    ports:
#      - "7233:7233"

  web:
    image: python:3.12-slim
    working_dir: /app
    command: sh -c "pip install --upgrade pip && pip install -r requirements.txt && python manage.py migrate && python manage.py runserver 0.0.0.0:8000 && python -m ExtractInfo.temporal_worker"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
#    depends_on:
#      - temporal
